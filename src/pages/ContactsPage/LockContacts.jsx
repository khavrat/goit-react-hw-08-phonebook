import AuthNav from "components/userMenu/AuthNav";

const LockContactsPage = () => {
    return (
      <section>
        {' '}
        <p>To enter the Contact you need to register or log in</p>
        <AuthNav />
      </section>
    );
};

export default LockContactsPage;
